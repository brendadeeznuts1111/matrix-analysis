# API Routes - RESTful endpoints
/api/v1/users/:id
/api/v1/users/:id(\d+)
/api/v2/projects/:projectId/tasks/:taskId

# Blog routes with date validation
/blog/:year(\d{4})/:month(\d{2})/:slug([a-z0-9-]+)
/blog/:year(\d{4})/:month(\d{2})

# E-commerce patterns
/products/:category/:id
/cart/items/:itemId
/checkout/:step(shipping|payment|review)

# Wildcard patterns
/files/*/:filename.:ext
/assets/**

# Potentially risky patterns (for security audit)
/admin/:path*
/api/internal/:endpoint
/:tenant/api/:resource
