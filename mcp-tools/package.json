{
  "name": "@tier1380/mcp-tools",
  "version": "1.3.7",
  "description": "Tier-1380 MCP Tool Registry with Bun.deepMatch validation",
  "main": "validate.ts",
  "type": "module",
  "scripts": {
    "test": "bun run test-validation.ts",
    "demo": "bun run demo.ts",
    "examples": "./quick-validation-examples.sh",
    "validate": "bun run test-validation.ts",
    "server": "bun run server.ts",
    "schema:validate": "bun -e 'const schema = require(\"./schema.json\"); console.log(\"✅ Schema valid\")'"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^0.5.0"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "keywords": [
    "mcp",
    "tier1380",
    "validation",
    "deepmatch",
    "bun"
  ],
  "author": "Tier-1380 OMEGA",
  "license": "MIT",
  "engines": {
    "bun": ">=1.3.6"
  }
}
