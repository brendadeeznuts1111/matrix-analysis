#!/usr/bin/env bun
/**
 * Quick preview of bun test CLI options matrix (renderCliMatrix).
 * @col_93 balanced_braces
 */

import { renderCliMatrix } from "./mcp-bun-docs/lib.ts";

console.log(renderCliMatrix());
