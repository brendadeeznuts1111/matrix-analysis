{
  "name": "matrix-cli",
  "version": "1.0.0",
  "type": "module",
  "engines": {
    "bun": ">=1.0.0"
  },
  "scripts": {
    "matrix:profile:use": "bun run src/cli.ts profile:use",
    "matrix:profile:list": "bun run src/cli.ts profile:list",
    "matrix:profile:show": "bun run src/cli.ts profile:show",
    "matrix:profile:diff": "bun run src/cli.ts profile:diff",
    "matrix:profile:create": "bun run src/cli.ts profile:create",
    "matrix:profile:export": "bun run src/cli.ts profile:export",
    "test": "bun test"
  },
  "devDependencies": {
    "@types/bun": "1.3.6",
    "bun-types": "1.3.6"
  },
  "dependencies": {
    "libphonenumber-js": "1.12.35"
  }
}
