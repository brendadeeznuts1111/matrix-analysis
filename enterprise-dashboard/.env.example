# Azure Artifacts token (don't base64 encode - Bun handles it)
NPM_PASSWORD=<paste token here>

# Or configure registry entirely via env var (alternative to bunfig.toml)
# NPM_CONFIG_REGISTRY=https://pkgs.dev.azure.com/<user>/_packaging/<user>/npm/registry/:username=<USERNAME>:_password=<PASSWORD>

# Test environment
NODE_ENV=test
